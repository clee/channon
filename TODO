 * cron-style scheduled execution support
 * eventsource endpoints
 * PUT support for /plans/:planName
 * move step/notification file generation and chmod to
   addPlan handler
 * tag support
 * inotify trigger?
