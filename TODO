 * cron-style scheduled execution support
 * eventsource endpoints
 * tag support
 * inotify trigger?

DONE:
 * move step/notification file generation and chmod to addPlan handler
 * PUT support for /plans/:planName
